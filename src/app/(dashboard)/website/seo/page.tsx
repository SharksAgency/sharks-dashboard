import { EntityPage } from "@/features/entities/entity-page"
export default function SeoPage() { return <EntityPage entity="site_settings" keys={["default_seo"]} overrideTitle={{ ar: "تحسين الظهور", en: "SEO" }} /> }
